import React from 'react';

import Modal from '../Modal';

const StreamDelete= () => {
    return (
      <div>
        Delete this Stream
        <Modal />
      </div>
    );
};

export default StreamDelete;